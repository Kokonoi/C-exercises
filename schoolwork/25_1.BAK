#include <stdio.h>
#include <math.h>
main()
{
	int x,y,z;
	printf("Unesi brojeve: \n");
	scanf("%d%d", &x,&y);
	while(x<=y)
	{
		z=pow(x, 2);
		printf("Kvadrat broja %6d je %6d\n", x,z);
		x++;

	}
	getch();

}