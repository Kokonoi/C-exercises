#include <stdio.h>

main()
{

 char c, tekst[100];
 int broj;
 gets(tekst);

 while ((c = getchar()) != EOF)
  {
   if (c == "a")
    broj++;
  }
 printf("Slovo a se pojavljuje %d puta.\n", broj);
 getch();

}
